# Обзор проекта: Веб-приложение для учета посещаемости и успеваемости студентов колледжа

## 📋 Описание проекта

Данное веб-приложение представляет собой систему учета посещаемости и успеваемости студентов колледжа. Система разработана как Single Page Application (SPA) без использования `react-router`, что обеспечивает быструю навигацию между разделами без перезагрузки страницы.

**Основная цель проекта**: Автоматизация учета посещаемости студентов и ведения журнала успеваемости с удобным веб-интерфейсом для преподавателей и администраторов.

## 🎯 Основные возможности системы

### Для всех пользователей (включая неавторизованных):

1. **Просмотр списка студентов**
   - Фильтрация по группам
   - Поиск по имени, фамилии, отчеству
   - Отображение информации о студентах

2. **Просмотр посещаемости**
   - Просмотр записей о посещаемости по датам и предметам
   - Фильтрация по группам и предметам

3. **Просмотр успеваемости**
   - Просмотр оценок студентов
   - Фильтрация по группам и предметам
   - Статистика по оценкам

### Для авторизованных пользователей (Преподаватель):

1. **Редактирование посещаемости**
   - Отметка посещения/непосещения студентов
   - Массовое редактирование посещаемости
   - Выбор даты и предмета для отметки

2. **Управление оценками**
   - Добавление, редактирование и удаление оценок
   - Указание типа оценки (зачет, экзамен, курсовая и т.д.)
   - Выбор предмета и даты выставления оценки

### Для администраторов:

Все функции преподавателя, плюс:

1. **Управление студентами** (CRUD операции)
   - Добавление новых студентов
   - Редактирование данных студентов
   - Деактивация студентов (без физического удаления)
   - Привязка студентов к группам

2. **Управление группами** (CRUD операции)
   - Создание и редактирование групп
   - Деактивация групп
   - Просмотр студентов в группе

3. **Управление предметами** (CRUD операции)
   - Добавление новых учебных предметов
   - Редактирование информации о предметах
   - Деактивация предметов

## 🛠 Технологический стек

### Frontend:

- **React 19.2.0** - библиотека для создания пользовательского интерфейса
- **TypeScript 5.9.3** - типизированный JavaScript для надежности кода
- **Vite (rolldown-vite 7.3.0)** - современный инструмент сборки
- **MobX 6.15.0** - библиотека для управления состоянием приложения
- **SCSS/SASS 1.97.1** - препроцессор CSS для стилизации
- **Firebase 12.7.0** - база данных (Firebase Realtime Database)

### Инструменты разработки:

- **ESLint** - линтер для проверки качества кода
- **TypeScript ESLint** - проверка типов TypeScript

## 📁 Архитектура и структура проекта

```
Class-Schedule_Nikita/
├── src/
│   ├── components/          # Переиспользуемые UI компоненты
│   │   ├── UI/             # Базовые компоненты (Button, Input, Modal, Table и т.д.)
│   │   ├── Layout/         # Компоненты макета (Header, Sidebar, MainLayout)
│   │   ├── LoginModal/     # Модальное окно авторизации
│   │   └── ConfirmModal/   # Модальное окно подтверждения действий
│   │
│   ├── pages/              # Страницы приложения
│   │   ├── HomePage/       # Главная страница с дашбордом
│   │   ├── StudentsPage/   # Страница списка студентов
│   │   ├── AttendancePage/ # Страница посещаемости
│   │   ├── GradesPage/     # Страница успеваемости
│   │   └── AdminPage/      # Страница администрирования
│   │
│   ├── store/              # Управление состоянием (MobX stores)
│   │   ├── AuthStore.ts    # Авторизация и роли пользователей
│   │   ├── DataStore.ts    # CRUD операции с данными
│   │   ├── NavigationStore.ts # Навигация между страницами
│   │   └── UIStore.ts      # UI состояние (модалки, уведомления)
│   │
│   ├── types/              # TypeScript типы и интерфейсы
│   │   ├── user.types.ts   # Типы пользователей и ролей
│   │   ├── student.types.ts # Типы студентов
│   │   ├── group.types.ts  # Типы групп
│   │   ├── subject.types.ts # Типы предметов
│   │   ├── attendance.types.ts # Типы посещаемости
│   │   ├── grade.types.ts  # Типы оценок
│   │   └── navigation.types.ts # Типы навигации
│   │
│   ├── styles/             # Глобальные стили
│   │   ├── globalColors.scss # Цветовая палитра (кофейная тема)
│   │   ├── variables.scss  # CSS переменные (отступы, шрифты, breakpoints)
│   │   ├── mixins.scss     # SCSS миксины для переиспользования
│   │   └── global.scss     # Глобальные стили и импорт Google Fonts
│   │
│   ├── hooks/              # Кастомные React хуки
│   │   ├── useDebounce.ts  # Дебаунс для поиска
│   │   ├── useLocalStorage.ts # Работа с localStorage
│   │   └── useMediaQuery.ts # Определение размера экрана
│   │
│   ├── utils/              # Утилиты
│   │   ├── formatters.ts   # Форматирование данных (даты, имена)
│   │   └── validators.ts   # Валидация форм
│   │
│   ├── firebase.ts         # Конфигурация Firebase и сервисы работы с БД
│   ├── App.tsx             # Главный компонент приложения
│   ├── main.tsx            # Точка входа приложения
│   └── reset.css           # Сброс стилей браузера
│
├── public/                 # Статические файлы
├── CREDENTIALS.md          # Учетные данные для входа (пароли)
├── OVERVIEW.md             # Этот файл - обзор проекта
└── package.json            # Зависимости и скрипты проекта
```

## 🔐 Система ролей и прав доступа

Проект использует систему ролей с разграничением прав доступа:

### Роли пользователей:

1. **user** (Гость) - роль по умолчанию
   - Просмотр студентов, посещаемости, успеваемости
   - Нет возможности редактирования данных

2. **teacher** (Преподаватель) - требует авторизацию
   - Все права гостя
   - Редактирование посещаемости
   - Управление оценками (добавление, редактирование, удаление)

3. **admin** (Администратор) - требует авторизацию
   - Все права преподавателя
   - Управление студентами (CRUD)
   - Управление группами (CRUD)
   - Управление предметами (CRUD)
   - Доступ к разделу "Администрирование"

**Пароли для входа** находятся в файле `CREDENTIALS.md`

## 🗄 Структура данных

### Основные сущности:

1. **Student (Студент)**
   - id, firstName, lastName, middleName
   - groupId (привязка к группе)
   - dateOfBirth, email, phone
   - isActive (активен/деактивирован)

2. **Group (Группа)**
   - id, name, description
   - year (год обучения)
   - isActive

3. **Subject (Предмет)**
   - id, name, description
   - code (код предмета)
   - isActive

4. **AttendanceRecord (Посещаемость)**
   - id, studentId, subjectId
   - date, status (present/absent/late/excused)
   - notes (примечания)

5. **Grade (Оценка)**
   - id, studentId, subjectId
   - value (значение оценки)
   - type (exam/test/coursework/etc.)
   - date (дата выставления)
   - notes

## 🚀 Запуск проекта

### Предварительные требования:

- Node.js (рекомендуется версия 18 или выше)
- npm (вероятнее всего Глобально установленный)

### Шаги запуска:

1. **Установка зависимостей:**
   ```bash
   npm install
   ```

2. **Запуск development сервера:**
   ```bash
   npm run dev
   ```
   
   Приложение будет доступно по адресу: `http://localhost:5173/<НАЗВАНИЕ_ПРОЕКТА>/`

## 💻 Как работать с проектом (если захотите кастомизировать, что-то поменять)

### Навигация

Система использует кастомную навигацию без `react-router`. Все навигационные операции выполняются через `NavigationStore`:

- Используйте `navigationStore.navigate('page-id')` для перехода на страницу
- Текущая страница хранится в `navigationStore.currentPage`
- Навигация автоматически проверяет права доступа пользователя

### Работа с данными

Все операции с данными (CRUD) выполняются через `DataStore`:

- Методы начинаются с `load` - загрузка данных
- Методы начинаются с `create` - создание новой записи
- Методы начинаются с `update` - обновление записи
- Методы начинаются с `delete` - удаление/деактивация записи

Пример:
```typescript
import { dataStore } from '@/store';

// Загрузка всех студентов
dataStore.loadStudents();

// Создание нового студента
dataStore.createStudent({
  firstName: 'Иван',
  lastName: 'Иванов',
  // ... другие поля
});

// Фильтрация студентов
dataStore.setFilters({ groupId: 'group-1', search: 'Иванов' });
const filtered = dataStore.filteredStudents;
```

### Работа с авторизацией

Авторизация управляется через `AuthStore`:

```typescript
import { authStore } from '@/store';

// Проверка роли
if (authStore.isAdmin) {
  // Администратор
}

// Проверка прав
if (authStore.canEditAttendance) {
  // Можно редактировать посещаемость
}

// Вход в систему (через модальное окно)
authStore.openLoginModal();

// Выход
authStore.logout();
```

### Стилизация

Проект использует модульный подход к стилям:

- **Глобальные стили**: `src/styles/global.scss`, `globalColors.scss`, `variables.scss`
- **Модульные стили**: каждый компонент имеет свой `.module.scss` файл
- **Цветовая палитра**: определена в `globalColors.scss` (можно менять на свой вкус и цвет)

Для изменения цветовой схемы всего сайта достаточно изменить файл `globalColors.scss`.

## 📚 Что желательно понимать/почитать одним глазком для защиты диплома

### Теоретическая часть:

1. **SPA (Single Page Application)**
   - Что такое SPA и его преимущества? Ответ: SPA - это веб-приложение, которое загружается один раз, а затем все переходы между страницами происходят без перезагрузки всего сайта. Преимущества: быстрая работа, плавные переходы, меньше нагрузки на сервер, лучший пользовательский опыт.
   - Почему выбрана архитектура без react-router? Ответ: бесплатный GitHub не позволяет рендерить несколько страниц в виде отдельных компонентов. Поэтому для реализации был выбран подход SPA.
   - Как реализована навигация? Ответ: Навигация реализована через NavigationStore (MobX store), который хранит текущую страницу и переключает компоненты в App.tsx через switch-case. При изменении currentPage отрисовывается соответствующий компонент страницы.

2. **State Management (MobX)**
   - Зачем нужен MobX? Ответ: MobX нужен для управления состоянием приложения - хранения данных (студенты, группы, посещаемость) и их изменения. Без него пришлось бы передавать данные через множество компонентов, что усложняет код. MobX делает данные доступными из любого компонента.
   - Как организованы stores? Ответ: В проекте четыре основных store: AuthStore (авторизация и роли), DataStore (данные студентов, групп, посещаемости, оценок), NavigationStore (навигация между страницами), UIStore (модальные окна и уведомления). Каждый store отвечает за свою область.

3. **TypeScript**
   - Преимущества типизации? Ответ: TypeScript помогает на этапе написания кода находить ошибки до запуска программы. Компилятор проверяет типы данных и указывает на несоответствия, что экономит время на поиск ошибок.
   - Как типы помогают избежать ошибок? Ответ: Например, если функция ожидает строку, а мы передаем число, TypeScript сразу покажет ошибку. Это предотвращает ошибки вроде попытки вызвать метод у undefined или передачи неправильных данных в функцию.

4. **Firebase Realtime Database**
   - Что такое Firebase? Ответ: Firebase - это платформа от Google для разработки приложений. Firebase Realtime Database - это облачная NoSQL база данных, которая хранит данные в формате JSON и автоматически синхронизирует изменения между всеми клиентами в реальном времени.
   - Как организована структура данных? Ответ: Данные разделены на разделы: students, groups, subjects, attendance, grades. Каждая запись имеет уникальный идентификатор.
   - Преимущества и недостатки NoSQL базы данных? Ответ: Преимущества: гибкая структура данных, быстрая работа с большими объемами, простота масштабирования, удобно для веб-приложений. Недостатки: нет строгих связей между данными (как в SQL), сложнее делать сложные запросы, нужно самостоятельно контролировать целостность данных.

5. **Архитектура приложения**
   - Разделение на компоненты, страницы, stores? Ответ: Компоненты (UI) - переиспользуемые элементы интерфейса (кнопки, формы, таблицы). Страницы - полные экраны приложения, которые используют компоненты. Stores - хранилища данных и логики, которые используются страницами и компонентами. Такое разделение позволяет легко изменять код: например, изменить кнопку в одном месте, и она изменится везде. 

### Практическая часть (что можно продемонстрировать):

1. **Работа с ролями:**
   - Войти как преподаватель/администратор
   - Показать разницу в правах доступа

2. **CRUD операции:**
   - Добавить студента/группу/предмет
   - Отредактировать данные
   - Деактивировать запись

3. **Фильтрация и поиск:**
   - Поиск студентов по имени
   - Фильтрация по группам
   - Выбор даты для посещаемости

4. **Адаптивность:**
   - Показать работу на мобильных устройствах
   - Мобильное меню

5. **Валидация:**
   - Попытка создать запись с некорректными данными
   - Отображение ошибок

### Возможные вопросы:

1. **"Почему нет react-router?"**
   - Проект не требует глубокой вложенности маршрутов
   - Кастомная навигация проще для данного случая
   - Быстрая переключение между страницами без перезагрузки

2. **"Как обеспечивается безопасность?"**
   - Проверка прав на уровне навигации
   - Валидация данных на клиенте
   - Разграничение доступа по ролям

3. **"Как можно расширить функционал?"**
   - Добавление экспорта данных (Excel, PDF)
   - Уведомления
   - Календарь занятий
   - Статистика и отчеты
   - Интеграция с другими системами

4. **"Какие были сложности при разработке?"**
   - Организация структуры данных
   - Синхронизация состояния между компонентами
   - Реализация фильтрации и поиска
   - Адаптивный дизайн

## 📞 Дополнительная информация

- **Документация по паролям**: см. `CREDENTIALS.md`
- **Как показать преподу БД**: Что-нибудь придумаем, но сама бд ваша располагается здесь `https://console.firebase.google.com/u/0/project/college-diplomas/database/college-diplomas-default-rtdb/data/~2F<ПОРЯДКОВЫЙ_НОМЕР_В_ДОКЕ>-<НАЗВАНИЕ_ПРОЕКТА>-<ВАШЕ_ИМЯ>`
- **Тема диплома**: "Разработка веб-приложения для учета посещаемости и успеваемости студентов колледжа"

---

**Удачи на защите! 🎉**
